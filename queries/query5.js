/**
 * Query 5: Create a structure to retrieve all tweets for a specific user.
 * - Uses Redis Lists (tweets:<screen_name>) to store tweet IDs per user.
 * - Uses Redis Hashes (tweet:<id>) to store tweet attributes.
 */

